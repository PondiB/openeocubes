% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processes-ml.R
\name{mlm_random_forest_envelope}
\alias{mlm_random_forest_envelope}
\title{Random forest for classification or regression}
\usage{
mlm_random_forest_envelope(
  max_variables,
  num_trees,
  seed = NULL,
  classification
)
}
\arguments{
\item{max_variables}{String. Number of features to consider when looking for the best split (e.g., \code{"sqrt"} or a numeric value).}

\item{num_trees}{Integer. Number of trees in the forest.}

\item{seed}{Integer, optional. Random seed for reproducibility.}

\item{classification}{Logical. If \code{TRUE}, sets up Random Forest for classification; if \code{FALSE}, for regression.}
}
\value{
A list of model parameters for caret, including:
\itemize{
\item \code{method}: \code{"rf"}
\item \code{tuneGrid}: a \code{data.frame} with the \code{mtry} value
\item \code{trControl}: a \code{trainControl} object with 5-fold cross-validation
\item \code{ntree}: number of trees
\item \code{seed}: the random seed (if provided)
}
}
\description{
Model case for the random forest. Creates a parameter list and trainControl
settings for Random Forest models (classification or regression) with specified
number of trees and splitting criteria.
}
\examples{
# Classification with 100 trees, sqrt(max_variables)
params <- mlm_class_random_forest(
                            max_variables = "sqrt"
                            num_trees = 100,
                            seed = 42,
                            classification = TRUE)

# Regression with 200 trees, 5 features per split
params <- mlm_regr_random_forest(num_trees = 200,
                            max_variables = "5",
                            seed = 42,
                            classification = FALSE)

}
