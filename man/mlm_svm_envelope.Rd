% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processes-ml.R
\name{mlm_svm_envelope}
\alias{mlm_svm_envelope}
\title{Support Vector Machine for classification or regression}
\usage{
mlm_svm_envelope(
  kernel,
  C = 1,
  gamma = 1,
  degree = 3,
  coef0 = 0,
  tolerance = 0.001,
  cachesize = 1000,
  seed = NULL,
  classification
)
}
\arguments{
\item{kernel}{Character. Kernel type to use: \code{'rbf'}, \code{'linear'} or \code{'poly'}.}

\item{C}{Numeric. Regularization parameter (cost). Must be > 0.}

\item{gamma}{Numeric, optional. Kernel coefficient for \code{'rbf'} and \code{'poly'}. Controls nonlinearity.}

\item{degree}{Integer, optional. Degree of the polynomial kernel (used only with \code{'poly'}).}

\item{coef0}{Numeric, optional. Independent term in the kernel function (used for \code{'poly'}).}

\item{tolerance}{Numeric, optional. Tolerance for termination criterion (default is 0.001).}

\item{cachesize}{Integer, optional. Size of the kernel cache in MB (default is 1000).}

\item{seed}{Integer or NULL, optional. Random seed for reproducibility. If NULL, no seed is set.}

\item{classification}{Logical. If \code{TRUE}, sets up SVM for classification; if \code{FALSE}, for regression.}
}
\value{
A list containing model parameters:
\itemize{
\item \code{method}: caret-compatible model identifier (\code{"svmRadial"}, \code{"svmLinear"}, \code{"svmPoly"})
\item \code{tuneGrid}: a data.frame with hyperparameter combinations
\item \code{trControl}: caret trainControl object with 5-fold cross-validation
\item \code{tolerance}: termination criterion tolerance
\item \code{cachesize}: kernel cache size in MB
\item \code{seed}: random seed (if provided)
\item \code{classification}: TRUE or FALSE
}
}
\description{
Model wrapper for Support Vector Machines (SVMs) using caret. Creates model setup and hyperparameter grid
for classification or regression with specified kernel type and tuning parameters.
Supports common kernel types including RBF, polynomial and linear.
}
\examples{
# Classification with RBF kernel
params <- mlm_class_svm(kernel = "rbf", C = 1, gamma = 0.1)

# Regression with polynomial kernel
params <- mlm_regr_svm(kernel = "poly", C = 0.5, degree = 4, gamma = 0.01, coef0 = 1)

}
